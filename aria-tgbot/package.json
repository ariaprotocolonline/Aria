{
  "name": "aria-tgbot",
  "version": "1.0.0",
  "private": true,
  "description": "ARIA Telegram bot — standalone notification and chat service",
  "main": "dist/index.js",
  "scripts": {
    "dev":   "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node":      "^20.0.0",
    "ts-node-dev":     "^2.0.0",
    "typescript":      "^5.0.0"
  }
}
